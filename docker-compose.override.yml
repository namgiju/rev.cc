# 로컬 HTML 개발: 기본 docker compose 명령에서 자동으로 적용된다.
# 저장된 파일을 직접 제공하므로 HTML/CSS/JS 변경 시 빌드할 필요가 없다.
# 이미지 자체를 실행하려면 docker compose -f docker-compose.yml up -d 를 사용한다.
services:
  frontend:
    volumes:
      - ./assignment-frontend:/usr/share/nginx/html:ro
