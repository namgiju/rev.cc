server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/revcc
    username: revcc
    password: revcc
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
