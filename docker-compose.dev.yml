# 선택적 로컬 개발용: 컨테이너 밖의 기존 Spring/Next 개발 환경에 DB를 연결한다.
# docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d postgres redis
services:
  postgres:
    ports:
      - "127.0.0.1:5433:5432"
  redis:
    ports:
      - "127.0.0.1:6379:6379"
