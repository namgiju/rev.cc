services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: revcc
      POSTGRES_USER: revcc
      POSTGRES_PASSWORD: revcc
    ports:
      - "5433:5432"
    volumes:
      - revcc_pg:/var/lib/postgresql/data
volumes:
  revcc_pg:
